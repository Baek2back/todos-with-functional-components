// eslint-disable-next-line no-unused-vars
import Justact from '../justact';
import styles from './Todos.module.css';

const Todos = () => {
  return <ul className={styles.todos}></ul>;
};

export default Todos;
